#include <string>

namespace ariel
{
    std::string snowman(int);
};

enum snowmanParts{
    HAT = 0,
    NOSE = 1,
    LEFT_EYE = 2,
    RIGHT_EYE = 3,
    LEFT_ARM = 4,
    RIGHT_ARM = 5,
    TORSO = 6,
    BASE = 7,
};

enum partOptions{
    OPTION_ONE = 1,
    OPTION_TWO = 2,
    OPTION_THREE = 3,
    OPTION_FOUR = 4,
};
